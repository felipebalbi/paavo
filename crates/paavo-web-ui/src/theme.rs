//! Light/dark theme: read, apply, toggle, and the sun/moon toggle button.
//!
//! Filled in by Task 4.4 (CSS + theme). The active theme is a `dark` class on
//! `<html>`, persisted in `localStorage`, defaulting to the OS
//! `prefers-color-scheme`.
