//! Typed `fetch` wrappers over the paavo-web JSON API (`/api/...`).
//!
//! Filled in by Task 4.2 (typed API client). Same-origin relative URLs:
//! the SPA is served by the very backend it calls, so `/api/jobs` resolves
//! to that backend with no base-URL configuration.
